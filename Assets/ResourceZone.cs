using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResourceZone : MonoBehaviour
{
    /*
    public GameManager gameManager; // Assign in Inspector

    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("Iron ore") || other.CompareTag("Lunar rocks")) // Ensure the ore is detected
        {
            OreMining oreScript = other.GetComponent<OreMining>();
            if (oreScript != null)
            {
                oreScript.SetCurrentZone(this);
            }
        }
    }

    private void OnTriggerExit(Collider other)
    {
        if (other.CompareTag("Ore"))
        {
            OreMining oreScript = other.GetComponent<OreMining>();
            if (oreScript != null)
            {
                oreScript.ClearCurrentZone();
            }
        }
    }
    */
}
